<div class="product span4">
  
  <!-- Begin product image -->
  <div class="image">
    <a href="{{ collection.url }}">
      {% if collection.image %}
        <img data-src="{{ collection.image.src | collection_img_url: 'large' }}" alt="{{ collection.image.alt }}" class="lazyload" />
      {% else %}
        <img data-src="{{ collection.products.first.featured_image | product_img_url: 'large' }}" alt="{{ collection.title | escape  }}" class="lazyload" />
      {% endif %}
    </a>
  </div>
  <!-- End product image -->
  
  <!-- Begin product description -->
  <div class="details">
    <a href="{{ collection.url }}" class="clearfix">
      <h4 class="title">{{ collection.title }}</h4>
      <span class="price">{{ collection.products_count }} {{ 'collections.general.items_with_count' | t: count: collections.general.items_with_count }}</span>
    </a>
  </div>
  <!-- End product description -->
  
</div>

{% cycle 'clear-collection-list': '', '', '<div style="clear:both;"></div>' %}